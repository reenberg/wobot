- Use NesC network types. Seems to be a bug with NesC and typedefs here...

- Use windows for detecting sequence number conflicts. Make initial sequence
  number random. DONE

- Send unsubscribe requests directly to publishers if they are known.

- Add support for timing out and refreshing subscriptions.

- Add exponential backoff when there is an ACK failure.

- Add TTL (scoping) for publish/subscribe requests.

- Don't send unnecessary duplicate packets in the multicast case.

- Make multicast destination resend data instead of swallow it.

- Eject old entries in link estimator's neighbor table.

