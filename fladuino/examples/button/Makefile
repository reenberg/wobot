## These settings can be individually set, but if not they are set to default
## values in common.mk

## PLATFORM is default set to: duemilanove and specifyes the arduino platform
## which is going to be the target. See supported platforms in common.mk. 
## For non supportet platforms set the MCU and UPLOAD_RATE accordingly
# PLATFORM = 

## PORT is default set to: /dev/ttyUSB0 except for the bt platform which sets it
## to /dev/rfcom0
# PORT = 

## PROGNAME is default set to: $(notdir $(CURDIR)) and specifyes the name of the
## resulting .pde and .hex files which is generated from Main.hs
# PROGNAME = 


include ../common.mk
